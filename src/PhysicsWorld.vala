namespace Physv {
    public class PhysicsWorld {
        public const float MINIMUM_BODY_SIZE = 0.01f * 0.01f;
        public const float MAXIMUM_BODY_SIZE = 64f * 64f;

        // g/cm^3
        public const float MINIMUM_DENSITY = 0.2f;
        public const float MAXIMUM_DENSITY = 21.4f;
    }
}
